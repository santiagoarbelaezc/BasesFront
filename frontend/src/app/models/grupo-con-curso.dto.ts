// Representa un grupo académico vinculado a un curso,
// incluyendo el nombre del grupo y del curso asociado.
export interface GrupoConCursoDTO {
  grupo_id: number;
  nombre: string;
  curso_nombre: string;
}

